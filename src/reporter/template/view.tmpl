<b:style src="view.css"/>
<div id="layout">
  <div id="header">
    <span class="header-caption">Basis.js test runner</span>
    <div class="header-buttons">
      <button class="header-button" event-click="run">run</button>
      <button class="header-button" event-click="reset">reset</button>
    </div>
    <div class="header-test-suite-location">{done} / {total} ({assert}) (time: {time} ms)</div>
  </div>
  <div id="sidebar">
    <!--{toc}-->
  </div>
  <div id="content">
    <!--{tests}-->
  </div>
</div>
